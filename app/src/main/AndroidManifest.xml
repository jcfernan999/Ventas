<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ventas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ventas">
        <activity
            android:name=".vendedor.VendedorActivity"
            android:exported="false"
            android:label="@string/title_activity_vendedor"
            android:theme="@style/Theme.Ventas.NoActionBar" />
        <activity
            android:name=".cliente.ClienteActivity"
            android:exported="false"
            android:label="@string/title_activity_cliente"
            android:theme="@style/Theme.Ventas.NoActionBar" />
        <activity
            android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>